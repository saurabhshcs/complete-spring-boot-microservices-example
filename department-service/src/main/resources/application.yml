server:
  port: 9001

eureka:
    client:
      register-with-eureka: true
      fetch-registry: true
      service-url: http://localhost:8761
    instance:
      hostname: localhost

spring:
  application:
    name: DEPARTMENT-SERVICE